# Ignore everything - just in case HA adds something
*

# But not these files...
!.gitignore
!automations.yaml
!configuration.yaml
!README.md

#for some reason both lines are required, as excluding subdirectories required first excluding parent directory
!global_variables 
!global_variables/*